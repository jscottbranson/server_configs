#!/bin/bash
cd ~/server_confs
cp -sf .vimrc /root/.vimrc
cp .tmux.conf /root/.tmux.conf
chmod 500 ~/.vimrc
chmod 500 ~/.tmux
echo If there weren\'t any error messages, then all symbolic links were created successfully.
